[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-contract"
version = "0.1.0"
description = "RAG pipeline for contract intelligence"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["app"]
